[project]
description     = "Python devops tooling for Dango"
name            = "dango"
readme          = "README.md"
requires-python = ">=3.14"
version         = "0.0.0"

dependencies = [
  "ansible>=13.6.0",
]
