# getAllPairStats

Read 24h stats for every trading pair.

:::warning[DEX currently disabled]
The Dango DEX is currently disabled. Calls described on this page will not execute on the live network until the DEX is enabled.
:::


## Signature

```ts
function getAllPairStats(client: Client): Promise<PairStats[]>
```

## Example

```ts
import { createPublicClient, createTransport, testnet } from "@left-curve/sdk"

const client = createPublicClient({ chain: testnet, transport: createTransport() })
const stats = await client.getAllPairStats()
```

## Returns

**`PairStats[]`** — one `{ baseDenom, quoteDenom, currentPrice, price24HAgo, volume24H, priceChange24H }` per pair.

## See also

- [`getPairStats`](./getPairStats) — single-pair variant
- [`allPairStatsSubscription`](../indexer/allPairStatsSubscription) — live updates
