# vaultAddLiquidity

Deposit into the perps vault. Optional minimum-shares-to-mint slippage control.

## Signature

```ts
function vaultAddLiquidity(
  client: Client<Signer>,
  parameters: {
    sender: Address
    amount: string
    minSharesToMint?: string
  },
): Promise<{ hash: Uint8Array } & TxData>
```

## Example

```ts
import { createSignerClient, createTransport, testnet, PrivateKeySigner } from "@left-curve/sdk"
import type { Address } from "@left-curve/sdk"

const client = createSignerClient({
  chain: testnet,
  transport: createTransport(),
  signer: PrivateKeySigner.fromMnemonic(process.env.DANGO_MNEMONIC!),
})
const sender: Address = "0x1234567890abcdef1234567890abcdef12345678"

await client.vaultAddLiquidity({
  sender,
  amount: "1000000000",
  minSharesToMint: "950000",
})
```

## Parameters

**`sender`** — `Address`. The depositor.

**`amount`** — `string`. Deposit amount in base units.

**`minSharesToMint`** — `string`, optional. Minimum vault shares the deposit must mint.

## Returns

**`{ hash: Uint8Array } & TxData`** — see [`broadcastTxSync`](../app/broadcastTxSync).

## See also

- [`vaultRemoveLiquidity`](./vaultRemoveLiquidity)
- [`getPerpsVaultState`](./getPerpsVaultState)
