subscription SubscribeAccounts($userIndex: Int, $sinceBlockHeight: Int) {
  accounts(userIndex: $userIndex, sinceBlockHeight: $sinceBlockHeight) {
    accountIndex
    address
    createdBlockHeight
    createdTxHash
    createdAt
    users {
      userIndex
    }
  }
}
