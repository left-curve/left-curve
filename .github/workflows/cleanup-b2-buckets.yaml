name: Cleanup Empty B2 Buckets

permissions: {}

on:
  schedule:
    # every day at 4am UTC (offset from GHCR cleanup at 3am)
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: [self-hosted]
    environment: deploy
    env:
      ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Install Ansible
        run: |
          pipx install ansible
          echo "${HOME}/.local/bin" >> $GITHUB_PATH
          ln -sf ~/.local/pipx/venvs/ansible/bin/ansible-galaxy ~/.local/bin/ansible-galaxy
          ln -sf ~/.local/pipx/venvs/ansible/bin/ansible-playbook ~/.local/bin/ansible-playbook

      - name: Install collections
        run: |
          ansible-galaxy collection install -r deploy/requirements.yml

      - name: Start SSH agent
        uses: webfactory/ssh-agent@836c84ec59a0e7bc0eabc79988384eb567561ee2 # v0.7.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      - name: Cleanup empty B2 buckets
        run: |
          cd deploy
          ansible-playbook cleanup-b2-buckets.yml --limit 100.96.253.40
