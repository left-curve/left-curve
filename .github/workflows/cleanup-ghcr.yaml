name: Cleanup Old GHCR Images
on:
  schedule:
    # every day at 3am UTC
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  cleanup:
    permissions:
      packages: write
    runs-on: ubuntu-latest
    steps:
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Cleanup old container images
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          DAYS: 5
        run: |
          set -e

          echo "Starting cleanup for repository: $REPO"
          echo "Deleting images older than $DAYS days"
          echo "Current date: $(date)"
          echo "Cutoff date: $(date -d "-${DAYS} days")"
          echo "====================================="

          IMAGES=("left-curve%2Fdango" "left-curve%2Fdango-frontend")
          TOTAL_PROCESSED=0
          TOTAL_DELETED=0
          TOTAL_SKIPPED=0
          TOTAL_ERRORS=0

          for IMAGE in "${IMAGES[@]}"; do
            echo "🧹 Processing image: $IMAGE"

            # Decode image name for docker commands (e.g., left-curve%2Fdango -> left-curve/dango)
            DECODED_IMAGE=$(echo "$IMAGE" | sed 's/%2F/\//g')

            # Initialize associative array to track protected digests
            # These are digests referenced by 'latest' or 'pr-*-latest' manifest lists
            declare -A PROTECTED_DIGESTS

            # Collect protected digests from 'latest' tag
            echo "  Collecting protected digests from 'latest' tag..."
            if latest_manifest=$(docker manifest inspect "ghcr.io/left-curve/${DECODED_IMAGE}:latest" 2>/dev/null); then
              # Extract all child manifest digests from the manifest list
              while IFS= read -r digest; do
                if [ -n "$digest" ]; then
                  PROTECTED_DIGESTS["$digest"]=1
                  echo "    Protected (from latest): $digest"
                fi
              done < <(echo "$latest_manifest" | jq -r '.manifests[]?.digest // empty' 2>/dev/null)
            else
              echo "    ⚠️  WARNING: Could not fetch 'latest' manifest for ${DECODED_IMAGE} (may not exist yet)"
            fi

            # Get all versions with proper pagination
            echo "  Fetching all versions..."
            page=1
            all_versions=""

            while true; do
              echo "    Fetching page $page..."

              page_response=$(gh api -H "Accept: application/vnd.github+json" \
                "/orgs/left-curve/packages/container/${IMAGE}/versions?page=${page}&per_page=100" 2>&1) || {
                echo "  ERROR: Failed to fetch page $page for $IMAGE"
                echo "  Response: $page_response"
                TOTAL_ERRORS=$((TOTAL_ERRORS + 1))
                break
              }

              # Check if page is empty
              page_count=$(echo "$page_response" | jq '. | length')
              if [ "$page_count" -eq 0 ]; then
                echo "    No more versions found"
                break
              fi

              echo "    Found $page_count version(s) on page $page"

              # Append to all_versions
              if [ -z "$all_versions" ]; then
                all_versions="$page_response"
              else
                all_versions=$(echo "$all_versions" "$page_response" | jq -s 'add')
              fi

              ((page++))

              # Safety break to avoid infinite loops
              if [ $page -gt 100 ]; then
                echo "    WARNING: Reached page limit (100), stopping"
                break
              fi
            done

            if [ -z "$all_versions" ]; then
              echo "  No versions found, skipping"
              continue
            fi

            # Count total versions
            total_count=$(echo "$all_versions" | jq '. | length')
            echo "  Total versions found: $total_count"

            # First pass: collect protected digests from pr-*-latest tagged versions
            echo "  Collecting protected digests from pr-*-latest tags..."
            while IFS= read -r version; do
              # Check if this version has a pr-*-latest tag
              if echo "$version" | jq -r '.metadata.container.tags[]?' 2>/dev/null | grep -q "^pr-.*-latest$"; then
                pr_tag=$(echo "$version" | jq -r '.metadata.container.tags[]?' 2>/dev/null | grep "^pr-.*-latest$" | head -1)
                # Fetch the manifest to get child digests
                if pr_manifest=$(docker manifest inspect "ghcr.io/left-curve/${DECODED_IMAGE}:${pr_tag}" 2>/dev/null); then
                  while IFS= read -r digest; do
                    if [ -n "$digest" ]; then
                      PROTECTED_DIGESTS["$digest"]=1
                      echo "    Protected (from ${pr_tag}): $digest"
                    fi
                  done < <(echo "$pr_manifest" | jq -r '.manifests[]?.digest // empty' 2>/dev/null)
                else
                  echo "    ⚠️  WARNING: Could not fetch manifest for ${pr_tag}"
                fi
              fi
            done < <(echo "$all_versions" | jq -c '.[]')

            echo "  Total protected digests: ${#PROTECTED_DIGESTS[@]}"

            # Process each version
            while IFS= read -r version; do
              TOTAL_PROCESSED=$((TOTAL_PROCESSED + 1))

              CREATED=$(echo "$version" | jq -r '.created_at')
              ID=$(echo "$version" | jq -r '.id')
              NAME=$(echo "$version" | jq -r '.name // "unnamed"')

              # Get tags safely
              TAGS=$(echo "$version" | jq -r '.metadata.container.tags[]? // empty' 2>/dev/null | tr '\n' ',' | sed 's/,$//')

              echo "    Processing version: $ID (name: $NAME, tags: ${TAGS:-none}, created: $CREATED)"

              # Skip if has latest tag
              if echo "$version" | jq -r '.metadata.container.tags[]?' 2>/dev/null | grep -q "^latest$"; then
                echo "      ✅ SKIP: Has 'latest' tag"
                TOTAL_SKIPPED=$((TOTAL_SKIPPED + 1))
                continue
              fi

              # Skip if has pr-X-latest pattern
              if echo "$version" | jq -r '.metadata.container.tags[]?' 2>/dev/null | grep -q "^pr-.*-latest$"; then
                echo "      ✅ SKIP: Has PR preview tag"
                TOTAL_SKIPPED=$((TOTAL_SKIPPED + 1))
                continue
              fi

              # Skip if this version's digest is referenced by a protected tag (latest or pr-*-latest)
              # The NAME field contains the digest (sha256:...)
              VERSION_DIGEST="$NAME"
              if [ -n "$VERSION_DIGEST" ] && [ "${PROTECTED_DIGESTS[$VERSION_DIGEST]+exists}" ]; then
                echo "      ✅ SKIP: Digest referenced by protected tag (latest or pr-*-latest)"
                TOTAL_SKIPPED=$((TOTAL_SKIPPED + 1))
                continue
              fi

              # Check age
              CREATED_TIMESTAMP=$(date -d "$CREATED" +%s 2>/dev/null) || {
                echo "      ⚠️  SKIP: Invalid date format: $CREATED"
                TOTAL_SKIPPED=$((TOTAL_SKIPPED + 1))
                continue
              }

              CUTOFF_TIMESTAMP=$(date -d "-${DAYS} days" +%s)

              if [ "$CREATED_TIMESTAMP" -lt "$CUTOFF_TIMESTAMP" ]; then
                echo "      🗑️  DELETE: Image is older than $DAYS days"

                delete_response=$(gh api --method DELETE \
                  "/orgs/left-curve/packages/container/${IMAGE}/versions/$ID" 2>&1) || {
                  echo "      ❌ ERROR: Failed to delete version $ID"
                  echo "      Response: $delete_response"
                  TOTAL_ERRORS=$((TOTAL_ERRORS + 1))
                  continue
                }

                echo "      ✅ SUCCESS: Deleted version $ID"
                TOTAL_DELETED=$((TOTAL_DELETED + 1))
              else
                echo "      ✅ SKIP: Image is newer than $DAYS days"
                TOTAL_SKIPPED=$((TOTAL_SKIPPED + 1))
              fi
            done < <(echo "$all_versions" | jq -c '.[]')

            echo "  Completed processing $IMAGE"
            echo

            # Clear protected digests for next image
            unset PROTECTED_DIGESTS
          done

          echo "====================================="
          echo "CLEANUP SUMMARY"
          echo "====================================="
          echo "Images processed: ${#IMAGES[@]}"
          echo "Versions processed: $TOTAL_PROCESSED"
          echo "Versions deleted: $TOTAL_DELETED"
          echo "Versions skipped: $TOTAL_SKIPPED"
          echo "Errors encountered: $TOTAL_ERRORS"

          if [ $TOTAL_ERRORS -gt 0 ]; then
            echo "⚠️  WARNING: Some operations failed. Check logs above."
            exit 1
          else
            echo "✅ SUCCESS: Cleanup completed successfully"
          fi

