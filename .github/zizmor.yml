rules:
  self-hosted-runner:
    ignore:
      # Self-hosted runners are intentionally used for builds and
      # deployments. The zizmor audit in rust.yml gates these jobs.
      - rust.yml
      - cometbft.yml
      - hyperlane.yml
      - faucet.yml
      - portal-deployment.yaml
      - cleanup-pr.yaml
