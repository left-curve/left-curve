- hosts: all
  become: true
  remote_user: debian
  gather_facts: false
  collections:
    - community.docker
  pre_tasks:
    - ansible.builtin.import_tasks: tasks/load-debian-root-secrets.yml
  roles:
    - docker
