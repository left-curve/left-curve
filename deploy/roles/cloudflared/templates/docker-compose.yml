services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate --metrics 0.0.0.0:2000 run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    env_file:
      - .env
    networks:
      - cloudflare
    healthcheck:
      test: ["CMD", "cloudflared", "--metrics", "0.0.0.0:2000", "tunnel", "ready"]
      interval: 5s
      timeout: 3s
      retries: 5

networks:
  cloudflare:
    external: true
