---
- include_tasks: cloudflare_lb_cleanup.yml
  when: lb_exclude | default({}) | length > 0

- include_tasks: cloudflare_tunnel.yml

- include_tasks: cloudflare_lb.yml
  loop: "{{ dango_networks }}"
  loop_control:
    loop_var: dango_network
  when: cloudflare_lb_enabled
