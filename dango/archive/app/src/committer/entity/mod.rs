//! Sea-orm entities for the tables the committer owns. Generated-style
//! (sea-orm-cli output shape), used for queries — the table DDL lives in
//! the sibling `migrations` module.

pub(super) mod projection_cursors;
